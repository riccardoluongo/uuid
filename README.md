simple UUID library written in C with its only dependency being openSSL for MD5 and SHA1 hashing.
To use, simply drop into your project and include the header file.

Probably will use my own implementation of the hashing algorithms in the future.
